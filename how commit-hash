[33mcommit 43ac20539f504193abd185c0736f4f83f2cd4966[m[33m ([m[1;36mHEAD[m[33m -> [m[1;32mmain[m[33m, [m[1;31morigin/main[m[33m)[m
Author: Admin <admin@example.com>
Date:   Sat Aug 29 21:19:45 2026 +0200

    Sync configuration settings

[1mdiff --git a/README.md b/README.md[m
[1mindex e745063..177864b 100644[m
[1m--- a/README.md[m
[1m+++ b/README.md[m
[36m@@ -63,12 +63,12 @@[m [mis not included in a range count. It also includes `COLOR COUNTS`: Black[m
 (`1, 4, 7, 10, 13, 16`), Gray (`2, 5, 8, 11, 14, 17`), and Red[m
 (`3, 6, 9, 12, 15, 18`). Zero is not assigned a color.[m
 [m
[31m-The console prints a `RANGE ALERT` once when a range has not appeared for more[m
[31m-than 9 consecutive captured draws. This means the alert triggers at 10[m
[31m-consecutive draws without that range. The alert resets after that range appears[m
[31m-again, and an ongoing alert is shown again when a partial session is restored.[m
[31m-Zero extends the absence streak of every range because it is outside all three[m
[31m-ranges.[m
[32m+[m[32mThe console prints a `RANGE ALERT` or `COLOR ALERT` once when a range or color[m
[32m+[m[32mhas not appeared for more than 9 consecutive captured draws. This means the[m
[32m+[m[32malert triggers at 10 consecutive draws without that group. The alert resets[m
[32m+[m[32mafter the group appears again, and an ongoing alert is shown again when a[m
[32m+[m[32mpartial session is restored. Zero extends every range and color absence streak[m
[32m+[m[32mbecause it is outside all three ranges and colors.[m
 [m
 During a session, the tracker stores an atomic checkpoint in `sessions/`. A[m
 restart synchronizes any checkpointed rows that were not yet written to[m
[1mdiff --git a/config.py b/config.py[m
[1mindex df66e43..2cf570a 100644[m
[1m--- a/config.py[m
[1m+++ b/config.py[m
[36m@@ -41,11 +41,11 @@[m [mSNAPSHOT_STABLE_READS = 3[m
 [m
 [m
 # -----------------------------------------------------[m
[31m-# Range alerts[m
[32m+[m[32m# Absence alerts[m
 # -----------------------------------------------------[m
 [m
[31m-# Alert after a range has been absent for more than this many draws.[m
[31m-RANGE_ABSENCE_ALERT_AFTER = 9[m
[32m+[m[32m# Alert after a range or color has been absent for more than this many draws.[m
[32m+[m[32mABSENCE_ALERT_AFTER = 9[m
 [m
 [m
 # -----------------------------------------------------[m
[1mdiff --git a/models/number_domain.py b/models/number_domain.py[m
[1mindex b4a69e4..59ee44c 100644[m
[1m--- a/models/number_domain.py[m
[1m+++ b/models/number_domain.py[m
[36m@@ -1,6 +1,6 @@[m
 """Authoritative Wheel of Fortune result-number domain."""[m
 [m
[31m-from collections.abc import Iterable[m
[32m+[m[32mfrom collections.abc import Callable, Iterable[m
 [m
 [m
 NUMBER_MIN = 0[m
[36m@@ -68,11 +68,29 @@[m [mdef range_absence_streaks(values: Iterable[object]) -> dict[str, int]:[m
     A zero is a valid game result but belongs to no range, so it extends the[m
     absence streak of all three display ranges.[m
     """[m
[31m-    streaks = {label: 0 for label, _ in NUMBER_BANDS}[m
[32m+[m[32m    return _group_absence_streaks(values, NUMBER_BANDS, number_band)[m
[32m+[m
[32m+[m
[32m+[m[32mdef color_absence_streaks(values: Iterable[object]) -> dict[str, int]:[m
[32m+[m[32m    """Return current consecutive-absence lengths for every result color.[m
[32m+[m
[32m+[m[32m    A zero is a valid game result but belongs to no color, so it extends the[m
[32m+[m[32m    absence streak of Black, Gray, and Red.[m
[32m+[m[32m    """[m
[32m+[m[32m    return _group_absence_streaks(values, NUMBER_COLORS, number_color)[m
[32m+[m
[32m+[m
[32m+[m[32mdef _group_absence_streaks([m
[32m+[m[32m    values: Iterable[object],[m
[32m+[m[32m    groups: tuple[tuple[str, range], ...],[m
[32m+[m[32m    classify: Callable[[object], str | None],[m
[32m+[m[32m) -> dict[str, int]:[m
[32m+[m[32m    """Return trailing absence lengths for a configured set of groups."""[m
[32m+[m[32m    streaks = {label: 0 for label, _ in groups}[m
     for value in values:[m
[31m-        observed_band = number_band(value)[m
[31m-        for label, _ in NUMBER_BANDS:[m
[31m-            if label == observed_band:[m
[32m+[m[32m        observed_group = classify(value)[m
[32m+[m[32m        for label, _ in groups:[m
[32m+[m[32m            if label == observed_group:[m
                 streaks[label] = 0[m
             else:[m
                 streaks[label] += 1[m
[1mdiff --git a/results.txt b/results.txt[m
[1mindex ce206b7..037fda0 100644[m
[1m--- a/results.txt[m
[1m+++ b/results.txt[m
[36m@@ -16,3 +16,10 @@[m [mPos | Draw ID             | Result[m
  11 | 12608290891         |      4[m
  12 | 12608290892         |      2[m
  13 | 12608290893         |     18[m
[32m+[m[32m 14 | 12608290894         |      1[m
[32m+[m[32m 15 | 12608290895         |     17[m
[32m+[m[32m 16 | 12608290896         |      9[m
[32m+[m[32m 17 | 12608290897         |      1[m
[32m+[m[32m 18 | 12608290898         |     12[m
[32m+[m[32m 19 | 12608290899         |     17[m
[32m+[m[32m 20 | 12608290900         |      8[m
[1mdiff --git a/tests/test_number_domain.py b/tests/test_number_domain.py[m
[1mindex c8f7943..151f531 100644[m
[1m--- a/tests/test_number_domain.py[m
[1m+++ b/tests/test_number_domain.py[m
[36m@@ -4,6 +4,7 @@[m [mfrom models.number_domain import ([m
     NUMBER_BANDS,[m
     NUMBER_COLORS,[m
     NUMBER_VALUES,[m
[32m+[m[32m    color_absence_streaks,[m
     color_counts,[m
     is_valid_number,[m
     number_band,[m
[36m@@ -98,6 +99,16 @@[m [mdef test_color_counts_follow_the_configured_number_mapping():[m
     }[m
 [m
 [m
[32m+[m[32mdef test_color_absence_streaks_reset_only_for_the_observed_color():[m
[32m+[m[32m    streaks = color_absence_streaks([1, 0, 2, 5, 3, 0])[m
[32m+[m
[32m+[m[32m    assert streaks == {[m
[32m+[m[32m        "Black": 5,[m
[32m+[m[32m        "Gray": 2,[m
[32m+[m[32m        "Red": 1,[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m
 def test_result_model_rejects_invalid_result_number():[m
     with pytest.raises(ValueError, match="0 to 18"):[m
         Result(draw_id="12608180151", number=-1)[m
[1mdiff --git a/tests/test_session_presenter.py b/tests/test_session_presenter.py[m
[1mindex 5d3f328..fc87e1c 100644[m
[1m--- a/tests/test_session_presenter.py[m
[1m+++ b/tests/test_session_presenter.py[m
[36m@@ -72,7 +72,32 @@[m [mdef test_presenter_reports_an_existing_absence_alert_after_restore(capsys):[m
 [m
     restore_output = capsys.readouterr().out[m
     assert "RANGE ALERT | 1-6 has not appeared for 10 consecutive draws." in restore_output[m
[32m+[m[32m    assert "COLOR ALERT | Black has not appeared for 10 consecutive draws." in restore_output[m
 [m
     presenter.result_recorded([*results, ("12608180161", 0)])[m
 [m
[31m-    assert "RANGE ALERT" not in capsys.readouterr().out[m
[32m+[m[32m    next_output = capsys.readouterr().out[m
[32m+[m[32m    assert "RANGE ALERT" not in next_output[m
[32m+[m[32m    assert "COLOR ALERT" not in next_output[m
[32m+[m
[32m+[m
[32m+[m[32mdef test_presenter_alerts_once_when_a_color_is_absent_for_more_than_nine_draws([m
[32m+[m[32m    capsys,[m
[32m+[m[32m):[m
[32m+[m[32m    presenter = SessionPresenter()[m
[32m+[m[32m    first_nine = [(str(12608180151 + index), 0) for index in range(9)][m
[32m+[m[32m    tenth = [*first_nine, ("12608180160", 0)][m
[32m+[m
[32m+[m[32m    presenter.result_recorded(first_nine)[m
[32m+[m[32m    assert "COLOR ALERT" not in capsys.readouterr().out[m
[32m+[m
[32m+[m[32m    presenter.result_recorded(tenth)[m
[32m+[m[32m    alert_output = capsys.readouterr().out[m
[32m+[m[32m    for color in ("Black", "Gray", "Red"):[m
[32m+[m[32m        assert ([m
[32m+[m[32m            f"COLOR ALERT | {color} has not appeared for 10 consecutive draws."[m
[32m+[m[32m            in alert_output[m
[32m+[m[32m        )[m
[32m+[m
[32m+[m[32m    presenter.result_recorded([*tenth, ("12608180161", 0)])[m
[32m+[m[32m    assert "COLOR ALERT" not in capsys.readouterr().out[m
[1mdiff --git a/tracker/session_presenter.py b/tracker/session_presenter.py[m
[1mindex f8261d6..4774933 100644[m
[1m--- a/tracker/session_presenter.py[m
[1m+++ b/tracker/session_presenter.py[m
[36m@@ -2,11 +2,12 @@[m
 [m
 from pathlib import Path[m
 [m
[31m-from config import LINE, RANGE_ABSENCE_ALERT_AFTER[m
[32m+[m[32mfrom config import ABSENCE_ALERT_AFTER, LINE[m
 from models.number_domain import ([m
     NUMBER_BANDS,[m
     NUMBER_COLORS,[m
     NUMBER_VALUES,[m
[32m+[m[32m    color_absence_streaks,[m
     color_counts,[m
     number_counts,[m
     range_absence_streaks,[m
[36m@@ -22,6 +23,7 @@[m [mclass SessionPresenter:[m
     def __init__(self):[m
         self._range_trend_history: list[tuple[int, ...]] = [][m
         self._alerted_absent_ranges: set[str] = set()[m
[32m+[m[32m        self._alerted_absent_colors: set[str] = set()[m
 [m
     def restore(self, results: list[tuple[str, int]]) -> None:[m
         """Rebuild display-only trend history after a checkpoint restore."""[m
[36m@@ -29,10 +31,12 @@[m [mclass SessionPresenter:[m
         for index in range(1, len(results) + 1):[m
             self._range_trend_history.append(self._range_counts(results[:index]))[m
         self._print_range_absence_alerts(results)[m
[32m+[m[32m        self._print_color_absence_alerts(results)[m
 [m
     def reset(self) -> None:[m
         self._range_trend_history = [][m
         self._alerted_absent_ranges = set()[m
[32m+[m[32m        self._alerted_absent_colors = set()[m
 [m
     def waiting_for_session(self) -> None:[m
         print()[m
[36m@@ -66,6 +70,7 @@[m [mclass SessionPresenter:[m
     ) -> None:[m
         self._print_range_trend(results)[m
         self._print_range_absence_alerts(results)[m
[32m+[m[32m        self._print_color_absence_alerts(results)[m
 [m
     @staticmethod[m
     def session_incomplete([m
[36m@@ -153,27 +158,54 @@[m [mclass SessionPresenter:[m
     def _range_absence_streaks(results: list[tuple[str, int]]) -> dict[str, int]:[m
         return range_absence_streaks(value for _, value in results)[m
 [m
[32m+[m[32m    @staticmethod[m
[32m+[m[32m    def _color_absence_streaks(results: list[tuple[str, int]]) -> dict[str, int]:[m
[32m+[m[32m        return color_absence_streaks(value for _, value in results)[m
[32m+[m
     @staticmethod[m
     def _color_counts(results: list[tuple[str, int]]) -> dict[str, int]:[m
         return color_counts(value for _, value in results)[m
 [m
     def _print_range_absence_alerts(self, results: list[tuple[str, int]]) -> None:[m
         """Alert once when a range first exceeds the absence threshold."""[m
[31m-        streaks = self._range_absence_streaks(results)[m
[31m-        for label, _ in NUMBER_BANDS:[m
[32m+[m[32m        self._print_absence_alerts([m
[32m+[m[32m            "RANGE",[m
[32m+[m[32m            NUMBER_BANDS,[m
[32m+[m[32m            self._range_absence_streaks(results),[m
[32m+[m[32m            self._alerted_absent_ranges,[m
[32m+[m[32m        )[m
[32m+[m
[32m+[m[32m    def _print_color_absence_alerts(self, results: list[tuple[str, int]]) -> None:[m
[32m+[m[32m        """Alert once when a color first exceeds the absence threshold."""[m
[32m+[m[32m        self._print_absence_alerts([m
[32m+[m[32m            "COLOR",[m
[32m+[m[32m            NUMBER_COLORS,[m
[32m+[m[32m            self._color_absence_streaks(results),[m
[32m+[m[32m            self._alerted_absent_colors,[m
[32m+[m[32m        )[m
[32m+[m
[32m+[m[32m    @staticmethod[m
[32m+[m[32m    def _print_absence_alerts([m
[32m+[m[32m        alert_type: str,[m
[32m+[m[32m        groups: tuple[tuple[str, range], ...],[m
[32m+[m[32m        streaks: dict[str, int],[m
[32m+[m[32m        alerted_groups: set[str],[m
[32m+[m[32m    ) -> None:[m
[32m+[m[32m        """Print a one-time alert for each group that crosses the threshold."""[m
[32m+[m[32m        for label, _ in groups:[m
             streak = streaks[label][m
             if streak == 0:[m
[31m-                self._alerted_absent_ranges.discard(label)[m
[32m+[m[32m                alerted_groups.discard(label)[m
                 continue[m
             if ([m
[31m-                streak > RANGE_ABSENCE_ALERT_AFTER[m
[31m-                and label not in self._alerted_absent_ranges[m
[32m+[m[32m                streak > ABSENCE_ALERT_AFTER[m
[32m+[m[32m                and label not in alerted_groups[m
             ):[m
                 print([m
[31m-                    f"RANGE ALERT | {label} has not appeared for {streak} "[m
[32m+[m[32m                    f"{alert_type} ALERT | {label} has not appeared for {streak} "[m
                     "consecutive draws."[m
                 )[m
[31m-                self._alerted_absent_ranges.add(label)[m
[32m+[m[32m                alerted_groups.add(label)[m
 [m
     def _print_range_trend(self, results: list[tuple[str, int]]) -> None:[m
         counts = self._range_counts(results)[m
